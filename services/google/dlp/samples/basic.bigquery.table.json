{
  "name": "{{_table}}",
  "project": "{{project}}",
  "dataset": "{{ref:minimal.bigquery.dataset.json:name}}",
  "schema": {
    "fields": [
      {
        "name": "sample_field",
        "type": "STRING"
      }
    ]
  }
}

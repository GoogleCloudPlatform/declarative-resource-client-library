{
  "name": "{{ref:basic.tenant_oauth_idp_config.json:name}}",
  "project": "{{ref:basic.tenant.json:project}}",
  "tenant": "{{ref:basic.tenant.json:name}}",
  "clientId": "{{client}}",
  "issuer": "issuer",
  "enabled": true,
  "clientSecret": "updatedsecret"
}

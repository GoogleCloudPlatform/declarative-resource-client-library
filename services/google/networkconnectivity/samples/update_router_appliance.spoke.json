{
  "name": "{{spoke}}",
  "project": "{{project}}",
  "location": "{{region}}",
  "labels": {
    "label-two": "value-two"
  },
  "description": "An updated sample spoke with a linked routher appliance instance",
  "hub": "projects/{{project}}/locations/global/hubs/{{ref:basic.hub.json:name}}",
  "linkedRouterApplianceInstances": {
    "instances": [{
      "virtualMachine": "{{ref:router_appliance.compute.instance.json:selfLink}}",
      "ipAddress": "10.0.0.2"
    }]
  }
}

{
  "project": "{{project}}",
  "location": "global",
  "name": "{{name}}",
  "description": "description",
  "endpoint": {
    "gkeCluster": {
      "resourceLink": "{{ref:basicservice.container.cluster.json:selfLink}}"
    }
  },
  "authority": {
    "issuer": "https://container.googleapis.com/v1/projects/{{ref:basicservice.container.cluster.json:project}}/locations/{{ref:basicservice.container.cluster.json:location}}/clusters/{{ref:basicservice.container.cluster.json:name}}"
  }
}

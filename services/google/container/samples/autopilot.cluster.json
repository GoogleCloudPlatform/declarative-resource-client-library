{
  "name": "{{cluster}}",
  "masterVersion": "-",
  "addonsConfig": {
    "horizontalPodAutoscaling": {
      "disabled": false
    }
  },
  "autopilot": {
    "enabled": true
  },
  "description": "A sample cluster with autopilot enabled.",
  "project": "{{project}}",
  "location": "{{region}}"
}

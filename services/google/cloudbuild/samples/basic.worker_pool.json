{
 "name": "{{resource_name}}",
 "location": "{{region}}",
 "project": "{{project}}",
  "workerConfig": {
    "machineType": "e2-standard-2",
    "diskSizeGb": 100,
    "noExternalIP": true
  },
  "networkConfig": {
    "peeredNetwork": "projects/{{project}}/global/networks/{{ref:first.compute.network.json:name}}"
  }
}

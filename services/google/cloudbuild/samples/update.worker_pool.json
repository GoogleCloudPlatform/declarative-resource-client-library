{
 "name": "{{ref:basic.worker_pool.json:name}}",
 "location": "{{region}}",
 "project": "{{project}}",
  "networkConfig": {
    "peeredNetwork": "projects/{{project}}/global/networks/{{ref:other.compute.network.json:name}}"
  },
 "workerConfig": {
   "machineType": "e2-standard-4",
   "diskSizeGb": 120,
   "noExternalIP": false
 }
}

{
  "name": "{{cluster_name}}",
  "location": "{{region}}",
  "project": "projects/{{project}}",
  "labels": {
   "my-cluster": "exists"
  },
  "config": {
    "gceClusterConfig": {
      "zone": "{{zone}}",
      "tags": ["foo", "bar"]
    }
  }
}

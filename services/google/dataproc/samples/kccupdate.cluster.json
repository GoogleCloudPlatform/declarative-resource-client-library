{
  "name": "{{cluster_name}}",
  "location": "{{region}}",
  "project": "{{project}}",
  "labels": {
   "label-one": "new-value"
  },
  "config": {
   "autoscalingConfig": {
     "policy": "{{ref:basic.autoscaling_policy.json:name}}"
   },
   "masterConfig": {
    "diskConfig": {
     "bootDiskSizeGb": 30,
     "bootDiskType": "pd-standard"
    },
    "machineType": "n2-standard-2",
    "numInstances": 1
   }
  }
}

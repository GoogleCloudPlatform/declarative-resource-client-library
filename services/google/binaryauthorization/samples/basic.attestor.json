{
  "name": "{{attestor}}",
  "project": "{{project}}",
  "description": "A basic binary authorization attestor",
  "userOwnedGrafeasNote": {
    "noteReference": "{{ref:basic.containeranalysis.note.json:name}}"
  }
}
